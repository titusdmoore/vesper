package main

import "core:fmt"
import "cli"
import "network"
import "base:runtime"
import "core:mem"

main :: proc() {
	// Because I have the memory of a goldfish, to run everything, odin run . -- test -c
	cli.parse_args()
}

